---
title: kkRPC
description: kkRPC Documentation
template: splash
hero:
  tagline: RPC Everything with TypeScript
  actions:
    - text: Get Started
      link: /guides/get-started/
      icon: right-arrow
    - text: LLM Docs
      link: https://docs.kkrpc.kunkun.sh/llms.txt
      icon: external
      variant: secondary
    - text: Source Code
      link: https://github.com/kunkunsh/kkrpc
      icon: external
      variant: minimal
    - text: Typedoc
      link: https://kunkunsh.github.io/kkrpc/
      icon: external
      variant: minimal
---

![kkRPC Banner](https://imgur.com/19XswxO.jpg)

import { Card, CardGrid } from "@astrojs/starlight/components"

`kkrpc` lets two JavaScript or TypeScript runtimes call each other through type-safe proxy objects. Expose an API on one side, wrap a transport on the other side, and call remote functions, nested methods, properties, and callbacks like local code.

The stable API is built on native `Transport<RPCMessage>` objects. The core entry is browser-safe; runtime transports and optional peer dependencies live in subpath exports.

## Features

<CardGrid stagger>
	<Card title="Type-safe proxies">
		Remote APIs keep TypeScript completion and checking, including nested methods and properties.
	</Card>
	<Card title="Bidirectional channels">
		Evented transports let both sides expose APIs and pass callback arguments.
	</Card>
	<Card title="Native transports">
		Use stdio, HTTP, WebSocket, workers, iframes, Chrome extension ports, Electron, Tauri, Socket.IO, RabbitMQ, Kafka, Redis Streams, or NATS.
	</Card>
	<Card title="Browser-safe core">
		`kkrpc` and `kkrpc/browser` avoid Node-only imports. Optional integrations stay behind explicit subpaths.
	</Card>
	<Card title="Validation, middleware, metadata">
		Standard Schema validation, interceptor chains, and request metadata are available as plugins and channel options.
	</Card>
	<Card title="Transfer, relay, inspector">
		Transfer ownership where supported, bridge two transports, or observe traffic.
	</Card>
	<Card title="Built for long-lived channels">
		Callbacks are reclaimed automatically, dropped connections reject pending calls immediately, and per-call timeouts and cancellation keep failures fast.
	</Card>
</CardGrid>

## Runtime Coverage

| Runtime or system | Entry points |
| --- | --- |
| Browser and Web Worker | `kkrpc/browser`, `kkrpc/worker`, `kkrpc/iframe` |
| Node.js, Deno, Bun processes | `kkrpc/stdio`, `kkrpc/deno` |
| HTTP and WebSocket servers | `kkrpc/http`, `kkrpc/ws`, `kkrpc/ws/hono`, `kkrpc/ws/elysia` |
| Desktop apps | `kkrpc/electron`, `kkrpc/tauri` |
| Browser extensions | `kkrpc/chrome-extension` |
| Message buses | `kkrpc/socketio`, `kkrpc/rabbitmq`, `kkrpc/kafka`, `kkrpc/redis-streams`, `kkrpc/nats` |

## AI Support

The repository includes agent skill docs for AI coding assistants. They describe the stable API, package layout, and language interop implementations.

```bash
cp -r skills/kkrpc ~/.claude/skills/
cp -r skills/kkrpc-migration ~/.claude/skills/
cp -r skills/interop ~/.claude/skills/
```

See the [`skills/`](https://github.com/kunkunsh/kkrpc/tree/main/skills) directory for details.
