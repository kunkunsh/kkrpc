/**
 * Published `kkrpc/http` entry for unary HTTP transports and handlers.
 *
 * Import this entry for request/response RPC over Fetch-compatible HTTP. Use a
 * bidirectional transport such as `kkrpc/ws` when callbacks or server-initiated
 * calls are required.
 * @module
 */
export * from "../transports/http.ts"
