/**
 * Published `kkrpc/superjson` entry for SuperJSON transport codecs.
 *
 * Import this entry when composing transports that need richer value encoding
 * than plain JSON, such as Date, Map, Set, or BigInt support.
 * @module
 */
export * from "../features/superjson.ts"
