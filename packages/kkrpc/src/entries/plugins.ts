/**
 * Published `kkrpc/plugins` entry for low-level plugin hook primitives.
 *
 * Import this entry when authoring custom request, handler, response, or error
 * plugins outside the default core import surface.
 */
export * from "../core/plugins.ts"
