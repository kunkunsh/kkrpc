/**
 * Published `kkrpc/transport` entry for transport primitives.
 *
 * Import this entry when building custom `Transport<RPCMessage>` adapters or
 * composing a wire-level platform and codec with `createTransport()`.
 * @module
 */
export * from "../core/transport.ts"
