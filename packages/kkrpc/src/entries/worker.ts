/**
 * Published `kkrpc/worker` entry for Web Worker transports.
 *
 * Import this entry from browser or Deno worker contexts to wrap main-thread and
 * worker-global `postMessage()` endpoints.
 */
export * from "../transports/worker.ts"
