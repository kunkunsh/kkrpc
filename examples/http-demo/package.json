{
	"name": "hono-demo",
	"scripts": {
		"hono": "bun run --hot src/hono.ts",
		"express": "bun run --hot src/express.ts",
		"bun": "bun run --hot src/bun.ts",
		"http": "bun run --hot src/http.ts",
		"fastify": "bun run --hot src/fastify.ts",
		"deno": "deno run -N --watch src/deno.ts",
		"client": "bun run --hot client.ts",
		"check-types": "tsc --noEmit"
	},
	"dependencies": {
		"@kksh/demo-api": "workspace:*",
		"express": "^4.21.1",
		"fastify": "^5.1.0",
		"hono": "^4.6.10",
		"kkrpc": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/express": "^5.0.0"
	}
}
